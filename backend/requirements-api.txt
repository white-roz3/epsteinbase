# Minimal requirements for API server only (no image processing)
# PyMuPDF, Pillow, pytesseract only needed for extraction scripts, not API
fastapi==0.104.1
uvicorn[standard]==0.24.0
asyncpg==0.29.0  # Use specific version that has wheels for Python 3.11
python-multipart==0.0.6
pydantic==1.10.13
boto3==1.34.0
httpx==0.25.2
python-dotenv==1.0.0



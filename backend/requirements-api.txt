# Minimal requirements for API server only (no image processing)
# PyMuPDF, Pillow, pytesseract only needed for extraction scripts, not API
fastapi==0.104.1
uvicorn[standard]==0.24.0
asyncpg>=0.29.3  # Latest version with Python 3.13 support
python-multipart==0.0.6
pydantic==2.9.2
boto3==1.34.0
httpx==0.25.2
python-dotenv==1.0.0


